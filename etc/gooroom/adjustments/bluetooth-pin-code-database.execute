#!/bin/bash

#
## Gnome-Bluetooth pin-code-database Default Settings
#

SRC_FILE=/usr/share/debian-system-adjustments/bluetooth/pin-code-database.xml
TARGET_FILE=/usr/share/gnome-bluetooth/pin-code-database.xml

if [[ -e $SRC_FILE ]] && [[ -e $TARGET_FILE ]]; then
  cp $SRC_FILE $TARGET_FILE
fi
