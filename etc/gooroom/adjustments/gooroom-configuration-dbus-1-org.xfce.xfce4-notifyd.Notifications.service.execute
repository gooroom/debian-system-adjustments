#!/bin/bash

#
#  - 최초 설치후 gooroom-greeter에서 nm-applet의 연결 편집시 동작하지 않는 오류 수정
#

XFCE4_NOTIFY_SERVICE=/usr/share/dbus-1/services/org.xfce.xfce4-notifyd.Notifications.service

if [ -e $XFCE4_NOTIFY_SERVICE ]; then
  sed -i -e 's@^SystemdService=@#SystemdService=@g' $XFCE4_NOTIFY_SERVICE
fi
